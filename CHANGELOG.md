# Changelog

## 4.4.0

- Hide depth levels (`fuckcc hide 1|2|3`): env scrub, persona system prompt, max terminal fingerprint cleanup
- Region persona via `--append-system-prompt-file` (English-only workstation note)
- Proxy strips geo-ish headers; optional `Accept-Language: en-US`
- Free multi-language prompt translation (any source → region surface language)
- App: pure white UI, round controls, folder picker, launch modes, i18n, translate toggle, hide depth, full guide

## 4.3.0

- Control panel app with Claude icon, workdir selection, permission modes
- Default launch injects `--dangerously-skip-permissions` when mode=bypass

## 4.2.0

- Launch modes: manual / acceptEdits / auto / plan / dontAsk / bypass / safe

## 4.1.0

- Free local translation default; no vendor cloud defaults
- Open-source scrub (no hard-coded third-party API brands)

## 4.0.0

- Process-local camouflage, PATH shim install, local reverse proxy, menubar prototype
